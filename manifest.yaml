default_target: "groups-all"

params:
  project: "vmklib"

includes:
- "config/includes/python.yaml"

configs:
- "local/configs"

variables:
- "local/variables"

compiles:
- name: "local"

renders:
- name: "README.md"
  output_dir: "."
  dependencies:
  - "compiles-local"

groups:
- name: "all"
  dependencies:
  - "groups-python"
