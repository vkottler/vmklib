default_target: "groups-all"

params:
  project: "vmklib"

includes:
- "config/includes/python.yaml"

configs:
- "local/configs"

variables:
- "local/variables"

templates:
- "local/templates"

compiles:
- name: "local"

renders:
- name: "README.md"
  output_dir: "."
  dependencies:
  - "compiles-local"
- name: "header.mk"
  output_dir: "{{project}}/data/data"
  dependencies:
  - "compiles-local"

groups:
- name: "all"
  dependencies:
  - "groups-python"
  - "renders-README.md"
  - "renders-header.mk"
